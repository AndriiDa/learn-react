import React from "react";
import HeroContent from './HeroContent'
import PostsCards from './PostsCards'

const Main = (props) => {
  return (
     <main>
       <HeroContent />
       <PostsCards />
      </main>
  );
};

export default Main